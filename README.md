# ecomerce

Get the folder into your PC
Unzip it
I used VS Code to run the project - with physical device connected i.e. my phone (first configure physical device- i configured using Android Studio)
Open terminal and cd to the root direct directory (same as "pubspec.yaml" file)
In terminal run the command "flutter run"
